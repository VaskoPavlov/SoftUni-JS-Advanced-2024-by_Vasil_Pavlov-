/**
 * Task: Write a JS function that takes one string parameter 
 * and prints on two lines the length of the parameter and 
 * then the unchanged parameter itself.
 */

/**
 * 
 * @param {string} input 
 */
function count (input){
    console.log(input.length);
    console.log(input);
}